import { createSlice } from '@reduxjs/toolkit';

const initialState = {};

const conversationsApi = createSlice({
    name: 'auth',
    initialState,
    reducers: {},
});
export const {} = conversationsApi.actions;
export default conversationsApi.reducer;
